# Australian Public Data MCP

Public-data retrieval and analysis support for any MCP-capable agent.
Australian official sources: ABS, RBA and DCCEEW. Pacific islands: Pacific Data Hub/SPC. Global context: World Bank, OECD, IMF and UN Comtrade.

Canonical entrypoints:
- README.md: installation, Codex/Claude Code setup, tools and limitations
- AGENT_SYSTEM_PROMPT.md: analyst guidance supplied during MCP initialization
- AGENTS.md: development rules and architecture
- .mcp.json: project stdio configuration
- ausdata_mcp/server.py: tool contracts

Workflow: search_catalog -> select candidate -> get_metadata -> retrieve -> agent reads the complete JSON at artifact_path, filters and analyses it with its own code.
Prefer in-conversation charts and answers. The retrieval JSON is working evidence; do not create extra PNG, CSV, Excel or report files by default unless the user requests an export or the harness needs a temporary display file.
If no suitable dataset is retrievable through this MCP, report that limit and ask before seeking data from websites, downloadable workbooks/CSVs or other APIs. Do not imply the publisher lacks the data.
The retrieve manifest is a bounded summary, not the full evidence. The agent must have access to the MCP server's artifact_path; there are no post-retrieval MCP tools.
For Pacific data, search with provider PDH; get_metadata with dimension browses codelists, and retrieve.sourceFilters selects source codes. Preserve unit multipliers, observation status and publication annotations from the saved JSON. Every source feeds one normalized live catalogue and FTS index, cached per session for 24 hours. Search reports stale/unavailable sources; dataset metadata remains a separate step.
The calling agent chooses calculations, charts, files and response format. No web app or model API key is needed.
