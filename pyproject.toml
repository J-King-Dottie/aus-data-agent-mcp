[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = ["runtime", ".venv-wsl", ".agents"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
"scripts/*.py" = ["E402"]
